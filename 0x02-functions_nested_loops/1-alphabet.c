#include <stdio.h>
/**
 * print_alphabet - print alphabet
 *
 * Return: void
 *
 */
int main(void)
{
	char ch;
	for(ch = 'a'; ch <= 'z'; ch++)
	   {
		   printf("%c\n", ch);
           }
        return(0);
}
