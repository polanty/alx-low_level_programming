#include <stdio.h>
/**
 * main - Entry point
 *
 * Description:main- prints outs a string to stdout.
 *
 * Return : Always 0 (Success).
 */
int main(void)
{  
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
